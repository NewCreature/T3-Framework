CC = gcc
CFLAGS = $(GLOBAL_CFLAGS) $(APP_CFLAGS) $(LINUX_CFLAGS)
PLATFORM_OBJECTS = $(LINUX_PLATFORM_OBJECTS)
PLATFORM_LIBS = $(LINUX_PLATFORM_LIBS)
DEPEND_LIBS = -lm
DEL_COMMAND = rm -f
DEL_FOLDER_COMMAND = rm -rf

include makefile.common
